import dotenv from 'dotenv';
import mariadb from 'mariadb';

export const DB_CONFIG = {
  host: 'localhost',
  port: 3306,
  user: 'root',
  password: '0709',
  database: 'library',
};

export const PORT = 5555;
